## League management service

This is a Process Centric Service that handles requests related to leagues management.
It get requests from the Web interface

Main task for a process centric service:
- Aggregazione dei dati : raccoglie e prepara unico JSON da fornire all'interfaccia grafica
- gestione del flusso: dopo ogni passaggio decide cosa succede, dopo ogni chiamata verifica se ci sono errori